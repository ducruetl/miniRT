#include "lights.h"

void apply_light(struct img_data *img, struct light_data *light)
{
    /*
    for (int i = 0; i < img->height; i++)
    {
        unsigned char *row = img->img_addr + i * img->line_length;
        for (int j = 0; j < img->width; j++)
        {
            
        }
        
    }
    */
}
